{
  "name": "lock_and_cache",
  "version": "6.0.0-alpha.4",
  "description": "Most caching libraries don't do locking, meaning that >1 process can be calculating a cached value at the same time. Since you presumably cache things because they cost CPU, database reads, or money, doesn't it make sense to lock while caching?",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "keywords": [
    "redlock",
    "lock",
    "cache",
    "redis"
  ],
  "authors": [
    "Madeline Davies <npm@madd.tech>",
    "Jeff Laughlin",
    "Eric Kidd <eric.kidd@faraday.io>"
  ],
  "maintainers": [
    "Eric Kidd <eric.kidd@faraday.io>"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/faradayio/lock_and_cache_js.git"
  },
  "scripts": {
    "build": "tsc",
    "fmt": "prettier --write .",
    "lint": "eslint lib/**/*.ts test/**/*.ts",
    "prepublish": "rm -rf dist && npm run build",
    "test": "LOCK_AND_CACHE_LOG_LEVEL=trace mocha --require ts-node/register test/**/*.ts"
  },
  "files": [
    "dist/lib/**/*.d.ts",
    "dist/lib/**/*.js"
  ],
  "devDependencies": {
    "@types/lru-cache": "^5.1.0",
    "@types/mocha": "^8.0.2",
    "@types/node": "^14.0.27",
    "@types/redis": "^2.8.25",
    "@typescript-eslint/eslint-plugin": "^3.8.0",
    "@typescript-eslint/parser": "^3.8.0",
    "eslint": "^7.6.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.4",
    "git-validate": "^2.1.4",
    "mocha": "^8.1.1",
    "prettier": "^2.0.5",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "pre-commit": [
    "lint"
  ],
  "dependencies": {
    "lru-cache": "^6.0.0",
    "redis": "^2.6.2"
  }
}
